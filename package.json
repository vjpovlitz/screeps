{
  "name": "screeps",
  "version": "1.0.0",
  "description": "Screeps scripts",
  "main": "main.js",
  "scripts": {
    "clean": "if exist dist rd /s /q dist",
    "watch": "chokidar \"*.js\" -i \"dist/**\" -c \"git add . && git commit -m \\\"Auto-update Screeps code\\\" && git push\"",
    "start": "npm run clean && npm run watch",
    "push": "git add . && git commit -m \"Update Screeps code\" && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^3.0.0"
  }
}

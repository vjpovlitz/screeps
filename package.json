{
  "name": "screeps",
  "version": "1.0.0",
  "description": "Screeps scripts",
  "main": "main.js",
  "scripts": {
    "push": "git add . && git commit -m \"Update Screeps code [%DATE%]\" && git push",
    "watch": "chokidar \"**/*.js\" -c \"npm run push\"",
    "start": "npm run watch",
    "deploy": "git add . && git commit -m \"Major update: %npm_config_msg%\" && git push",
    "quick": "git add . && git commit -m \"Quick fix\" && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "cross-env": "^7.0.3"
  }
}
